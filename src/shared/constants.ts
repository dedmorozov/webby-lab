export const BLOCKS_REGEXP = /\n\s*\n/g;
export const TITLE_REGEXP = /^Title:\s*(.+)$/im;
export const RELEASE_YEAR_REGEXP = /^Release Year:\s*(\d{4})$/im;
export const FORMAT_REGEXP = /^Format:\s*(.+)$/im;
export const ACTORS_REGEXP = /^ACTORS:\s*(.+)$/im;

export const DEFAULT_DEBOUNCE_TIME = 300;
